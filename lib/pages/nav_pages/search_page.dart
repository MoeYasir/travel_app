import 'package:flutter/material.dart';

class SeerachPage extends StatelessWidget {
  const SeerachPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text(
        "Search Page",
        style: TextStyle(color: Colors.black),
      ),
    );
  }
}
